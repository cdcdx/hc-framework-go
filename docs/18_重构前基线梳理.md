# 18. 重构前基线梳理

> **文档性质**：重构前结构基线（SSOT 补充文档）
> **生成日期**：2026-07-13
> **目的**：在动手重构前，固化"文档 vs 实际代码"的真实对照，作为后续重构的统一参照系。
> **权威源约定**：功能以 `02_需求规格说明书.md` 为准；接口以 `04_API参考.md` 为准；配置以 `05_配置参考.md` 为准；安全以 `11_安全与防护.md` 为准；现状以 `12_实现状态与路线图.md` 为准。本文档只描述"当前代码结构是什么"，不替代上述权威源。

---

## 一、项目规模（当前实际）

| 维度 | 数据 |
|------|------|
| 语言 / Go 版本 | Go ≥ 1.25 |
| 源码包数 | `internal/` 19 个包、`pkg/` 8 个包 |
| 源码文件 | `internal/` 184 个 `.go`、`pkg/` 16 个 `.go` |
| 测试覆盖 | 每个核心包均含 `*_test.go`（service 11、handler 9、middleware 9、repository 10、mq 数十个） |
| 文档 | `docs/` 18 篇 `.md`（含本文档为第 18 篇） |
| 部署清单 | `deployments/` 17 个 K8s 清单 |
| 迁移脚本 | `migrations/` 14 个 `.sql`（按 `<域>/<驱动>/` 分层） |

---

## 二、实际目录结构（重构前基线）

```
hc-framework-go/
├── cmd/server/main.go              # 薄入口：解析 -config → bootstrap.Run()
├── internal/
│   ├── bootstrap/                  # 组合根（装配 + 启动 + 优雅关闭）
│   │   ├── app.go                  #   生命周期编排（run / shutdown 11步）
│   │   ├── bootstrap.go            #   InitDatabases / SeedData / 降级桥接
│   │   └── bootstrap_test.go
│   ├── config/                     # 配置结构 + Manager（atomic.Pointer 热更新）
│   │   └── config.go
│   ├── middleware/                 # 12 源文件 / 9 测试（Recovery 在 trace.go 内）
│   │   ├── trace.go               #   Trace + Recovery()（gin.HandlerFunc）
│   │   ├── timeout.go             #   Timeout + Recovery 引用
│   │   ├── request_info.go        #   RequestInfo
│   │   ├── logger.go              #   RequestLogger
│   │   ├── cors.go                #   CORS + CSP 头
│   │   ├── metrics.go             #   Metrics
│   │   ├── ratelimit.go           #   RateLimit（令牌桶 / 多维度 / 热更新）
│   │   ├── auth.go                #   Auth（JWT 校验）
│   │   ├── circuitbreaker.go      #   CircuitBreaker（三态熔断）
│   │   ├── captcha.go             #   Captcha（认证路由级）
│   │   └── security.go            #   SecurityIPLimit（IP 级限流/封禁）
│   ├── handler/                    # 11 源 / 9 测试
│   │   ├── auth_handler.go  idle_handler.go  task_handler.go
│   │   ├── shop_handler.go  user_handler.go  captcha_handler.go
│   │   ├── health_handler.go  deps.go  helper.go
│   ├── service/                    # 14 源 / 11 测试
│   │   ├── auth_service.go  idle_service.go  task_service.go  shop_service.go
│   │   ├── jwt.go  log_service.go  account_lock.go
│   │   ├── heartbeat_flusher.go  points_outbox.go  scan_sharding.go
│   │   └── ...（其余 service 文件）
│   ├── repository/                 # 12 源 / 10 测试
│   │   ├── user_repo.go  mongo_user_repo.go  idle_repo.go  task_repo.go
│   │   ├── shop_repo.go  points_outbox_repo.go  event_dedup_repo.go
│   │   ├── log_repo.go  log_repo_es.go  login_repo.go  monitor_repo.go  monitor_repo_ch.go
│   ├── model/                      # 11 源（纯定义，无测试）
│   │   ├── user.go  idle.go  task.go  order.go  transaction.go
│   │   ├── points_outbox.go  event_dedup.go  login_record.go  log.go  monitor.go  errors.go
│   ├── cache/                      # 10 源 / 6 测试
│   │   ├── l1.go  l2.go  bloom.go  hotkey.go  strategy.go
│   │   ├── manager.go  adapter.go  serializer.go  errors.go
│   ├── db/                         # 9 源 / 1 测试
│   │   ├── mysql.go  postgres.go  sqlite.go  mongodb.go
│   │   ├── elasticsearch.go  clickhouse.go  rw_db.go  adapter.go  gorm_logger.go
│   ├── mq/                         # 跨 5 适配器的完整实现（kafka/rabbitmq/rocketmq/memory/noop）
│   │   ├── adapter.go  factory.go  kafka.go  kafka_consumer.go
│   │   ├── rabbitmq.go  rocketmq.go  memory.go  noop.go
│   │   ├── retry.go  dlq.go  consumer_progress.go  metrics.go  topics.go  ctx.go  data/
│   ├── event/                      # 事件类型 + Payload 契约
│   ├── cluster/                    # 选主（Redis 租约）
│   ├── degrade/                    # 读/写降级全局状态
│   ├── metrics/                    # Prometheus 指标 + Collector
│   ├── trace/                      # OpenTelemetry 兼容链路追踪
│   ├── scheduler/                  # 自研定时任务调度器
│   └── router/                     # 路由注册（router.go）
├── pkg/                            # 无业务依赖的公共库
│   ├── jwt/  bcrypt/  captcha/  response/  serializer/
│   ├── mail/  logger/  contextkeys/
├── config/                         # config.yaml / example / stress
├── migrations/                     # business / user / log / monitor（按域分层）
├── deployments/                    # 17 K8s 清单 + grafana 面板
├── scripts/                        # migrate.sh / k6 / 压测脚本
└── swagger/
```

---

## 三、文档体系与 SSOT 关系

| 编号 | 文档 | 角色 | 权威内容 |
|------|------|------|----------|
| 00 | 文档导航 | 索引 | SSOT 约定、文档地图 |
| 01 | 需求 | 产品视角基线 | 需求要点（被 02 覆盖处以 02 为准） |
| 02 | 需求规格说明书 | **权威 SRS** | 功能/非功能/数据/接口/配置/部署 最终规范 |
| 03 | 架构说明 | 架构权威 | 请求链路、级联、分片、选主 |
| 04 | API 参考 | **接口契约权威** | 完整端点/参数/错误码/示例 |
| 05 | 配置参考 | **配置字段权威** | 全量 YAML 字段手册 |
| 06 | 数据库迁移 | 迁移机制 | golang-migrate 分层规则 |
| 07 | 集群设备部署手册 | 百万设备部署 | 可独立分发 |
| 08 | Redis 集群部署指南 | Redis 专题 | |
| 09 | 可观测性与告警 | 指标/告警权威 | 指标 + 告警规则 |
| 10 | 运维命令手册 | 命令速查 | |
| 11 | 安全与防护 | **安全专题权威** | 限流/熔断/CAPTCHA/锁定/密钥 |
| 12 | 实现状态与路线图 | **现状对照** | 已实现✅ / 待完善🚧 / 路线 |
| 13 | 代码优化记录 | 缺陷与优化史 | 优化点、Kafka 限制 |
| 14 | 测试与开发指南 | 开发/测试 | |
| 15 | 数据库选型参考 | 横向对比 | |
| 16 | 队列选型参考 | 横向对比 | |
| 17 | 启动流程与生命周期 | 启动/优雅关闭 | 有序关闭 11 步 |
| 18 | 重构前基线梳理 | **本文档** | 当前代码结构基线（仅供重构参照） |

**关键文档依赖链**：`01(基线) → 02(权威SRS) → 04(接口) / 05(配置) / 11(安全)`；`03(架构) ← 12(现状)` 交叉引用。

---

## 四、文档基线结构 vs 实际代码演进差异（核心）

需求文档 §13 / §10.6 描述的是**早期基线结构**，代码已演进，差异如下（重构前需统一认知）：

| 文档基线描述 | 实际代码现状 | 差异说明 |
|------|------|------|
| `middleware/recovery.go` | 无独立文件，`Recovery()` 在 `trace.go:41` | ✅ 已核实，归并至 trace.go |
| `middleware` 仅 8 文件 | 实际 12 源文件（+9 测试） | 新增 `request_info`/`security`/`timeout` 等 |
| `service` 仅 4 文件 | 实际 14 源文件 | 新增 `jwt`/`log_service`/`account_lock`/`heartbeat_flusher`/`points_outbox`/`scan_sharding` |
| `pkg` 仅 6 子包 | 实际 8 子包 | 新增 `mail`/`logger`/`contextkeys` |
| `internal` 无 `degrade`/`scheduler`/`metrics`/`trace`/`cluster` | 均已存在 | 演进新增 |
| `deployments/` 6 清单 | 实际 17 清单 | 扩展至完整 K8s 栈 |
| `migrations/{mysql,postgres,sqlite}` | 实际 `<域>/<驱动>/`（business/user/log/monitor） | 按域分层重构 |
| `handler` 无 `captcha`/`deps` | 均已存在 | 演进新增 |

> **重构前结论**：代码比文档基线更完整，但**文档结构已过时**。重构时应以 `02(SRS)` 为权威功能基线、`04/05/11` 为接口/配置/安全权威、`12(实现状态)` 为现状真相，并同步更新 `01/02 §13/§10.6` 的结构描述。

---

## 五、模块职责映射（重构前）

| 包 | 职责 | 关键文件 | 依赖 |
|----|------|----------|------|
| `bootstrap` | 组合根：装配所有组件 + 生命周期 + 优雅关闭 | `app.go`(run/shutdown 11步)、`bootstrap.go`(InitDatabases) | 全部 |
| `config` | 配置结构 + `Manager`(atomic.Pointer 热更新) | `config.go`(Validate) | viper |
| `middleware` | 请求链：Timeout→Trace(+Recovery)→RequestInfo→Logger→CORS→Metrics→RateLimit→Auth→CircuitBreaker→Captcha→Security | 12 源文件 | config/cache/response |
| `handler` | 薄层：参数绑定→调 Service→统一响应 | 11 源文件 | service/repository |
| `service` | 业务逻辑：认证/挂机/任务/商城/积分Outbox/账号锁/心跳flush/分片 | 14 源文件 | repository/cache/mq/model/db |
| `repository` | 数据访问：GORM/Mongo/ES/CH 抽象 | 12 源文件 | db/model |
| `model` | 领域模型 + 错误码 + 数据表结构 | 11 文件 | — |
| `cache` | 三级缓存 + 策略引擎 + L1/L2/Bloom/HotKey | 10 源文件 | config/degrade |
| `db` | 多库适配器 + 读写分离 RWDB | 9 源文件 | gorm |
| `mq` | 5 适配器 Producer/Consumer + 重试/DLQ/幂等 | 37 文件 | event |
| `event` | 事件类型 + Payload 契约 | — | — |
| `cluster` | 选主（Redis 租约） | — | cache |
| `degrade` | 读/写降级全局状态 | — | — |
| `metrics` | Prometheus 指标 + Collector | — | cache |
| `trace` | OTel 兼容链路追踪 | — | — |
| `scheduler` | 自研定时任务 | — | — |
| `router` | 路由注册 | `router.go` | handler/middleware |
| `pkg` | 无业务依赖公共库（jwt/bcrypt/captcha/response/serializer/mail/logger/contextkeys） | 8 包 | — |

---

## 六、重构前已知限制（来自 12 §二 / 13 §六，附核实）

| 项 | 现状 | 重构影响 |
|----|------|----------|
| Recovery 中间件归属 | 已核实：`trace.go:41` 的 `Recovery()`，无独立文件 | 重构时保留归并或拆分均可，需文档一致 |
| Kafka 无消费组 | kafka-go v0.4.51 与 KRaft 4.0 不兼容，绕开 group | 升级客户端或保留无组直读 |
| DLQ 无可视化/补偿工具 | 仅本地 JSONL 落盘 | 运维工具待补 |
| CSRF 仅 CSP 头 | 无 SameSite/Header 检查 | Bearer 场景风险低 |
| 热点 Key 本地预取 | 由 L1 延长 TTL 替代 | 可优化 |
| 每日积分上限 TOCTOU | 事务外查额度，并发可能略超 | 需 SELECT FOR UPDATE |
| 文档结构过时 | 01/02 §13/§10.6 描述旧结构 | 重构同步更新 |

---

## 七、重构前建议优先梳理项（Backlog）

> 状态标记：✅ 已落地 / 🔲 待办。以下状态基于 2026-07-13 的三项收尾动作（service 拆分、TOCTOU 修复、文档同步）。

1. ✅ **确认 Recovery 中间件归属**：已在 `18` 号文档核实归属 `trace.go:41`；`01 §13` / `02 §10.6` 结构树删除虚构的 `recovery.go` 并补注 `Recovery()`，文档已同步。
2. ✅ **统一文档结构**：`01_需求.md §13` 与 `02_需求规格说明书.md §10.6` 的项目结构树已与实际代码对齐（`recovery.go`→`trace.go`、`db/router.go`→`db/rw_db.go`、`cache/lock.go`→`cache/l2.go`、pkg 补 `mail/logger/contextkeys`）。
3. **固化 SSOT 引用**：确保 `04`(接口)/`05`(配置)/`11`(安全) 与代码字段严格对应，消除文档漂移。
   - ✅ **路径维度（2026-07-14）**：docs/12、14 拆分前扁平 `internal/service/*_test.go` 已重映射到 auth/idle/shop/common/task 子包；05/11/01/02 结构树与失效路径（recovery.go→trace.go、router.go→rw_db.go、lock.go→l2.go、auth_service/scan_sharding/account_lock 子包化）此前已对齐；04 引用的 `model/errors.go` 核实存在。
   - 🔲 **字段/端点维度**：配置字段（`mapstructure` 标签）与 API 端点（vs handler/router）逐字段比对仍待办，见 [19_优化清单与后续建议.md](./19_优化清单与后续建议.md) §二 P2/P3。
4. **标记待重构风险点**：
   - ✅ **每日积分上限 TOCTOU**：已修复。改用 Redis Lua 原子封顶预占（`cache.Manager.TryAcquireDailyPoints` / `ReleaseDailyPoints`），`idle_service.settleSession` 改为"预占→确认/回退"，消除事务外 check-then-use 竞态（`cache/manager.go`、`repository/idle_repo.go`）。
   - 🔲 **Kafka 无消费组**：仍待办。
   - 🔲 **DLQ 无可视化**：仍待办。
5. ✅ **模块边界审查（service 拆分）**：原 `service` 包已按业务域拆分为五个子包 —— `service/auth`（auth_service/jwt/account_lock）、`service/idle`（idle_service/scan_sharding/heartbeat_flusher）、`service/task`（task_service）、`service/shop`（shop_service）、`service/common`（log_service/points_outbox/cursor/decode）。`AccountLockStore` 接口下沉至 `cache` 包以打破循环依赖；`noopUserRepo` 等测试桩按包补位。`go build`/`go vet`/`go test ./internal/...`（含 `-race`）全绿。
6. 🔍 **middleware 体积**：11 源文件（auth/captcha/circuitbreaker/cors/logger/metrics/ratelimit/request_info/security/timeout/trace）结构清晰，评估为**维持扁平、无需拆分**（见 [19_优化清单与后续建议.md](./19_优化清单与后续建议.md) §二 P3）。

---

## 八、生成说明

本文档由代码探查（目录扫描 + 关键字核实）生成，作为重构前的结构基线快照。后续任何重构动作应以本文档为对照起点，并在完成后回写"重构后结构"。
